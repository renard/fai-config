# Defaults for puppetmaster - sourced by /etc/init.d/puppetmaster

# Start puppetmaster on boot? If you are using passenger, you should
# have this set to "no"
START=no

# Startup options
DAEMON_OPTS=""

SERVERTYPE=passenger
PUPPETMASTERS=1
PORT=8141
DAEMON_OPTS="--ssl_client_header=HTTP_X_CLIENT_DN"